{
  "name": "boss-timers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"Nothing to build, static app\"",
    "build:web": "echo \"static web files\""
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}